plugins {
    id 'org.gradle.python.python' version '7.3'
}

pluginManagement {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
}


python {
    pythonVersion = '3.8'
    distName = 'python-gradle-test'
}

tasks.test {
    useJUnitPlatform()
}
